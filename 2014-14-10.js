function solution () {
    return {
    
        solveFor: function() {
        
        };
        
}

var a = solution() , b = solution();

var assert = require('chai').assert;

assert.typeOf(solution, 'function');
assert.typeOf(a, '?');
assert.typeOf(b, '?');
        